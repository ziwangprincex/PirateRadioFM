{
  "name": "pirate-radio",
  "version": "0.2.0",
  "description": "Radio, podcasts, Spotify, and Apple Music for CLI coding agents.",
  "type": "module",
  "bin": {
    "pirate-radio": "dist/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "start": "node dist/index.js",
    "check": "tsc --noEmit && node build.mjs && node --test dist/selfcheck.js",
    "check:stations": "node scripts/check-stations.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0"
  }
}
