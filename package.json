{
  "name": "pirate-radio",
  "version": "0.2.0",
  "description": "Radio + Spotify + podcasts + Apple Music MCP server for CLI coding agents — built-in genre stations, auto-stops when your session ends.",
  "type": "module",
  "bin": {
    "pirate-radio": "dist/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "start": "node dist/index.js",
    "check": "tsc --noEmit && node build.mjs && node dist/selfcheck.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0"
  }
}
